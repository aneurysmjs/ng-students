describe('Controller: AppController', function () {

   beforeEach(module('app'));

   var ctrl;

   beforeEach(inject(function ($controller) {
      ctrl = $controller('AppController');
   }));

   it('should have the array of students', function () {

   });

});